# authentication-system-php
This project is a simple authentication system developed using PHP, HTML, CSS, and MySQL. It allows users to register, log in, and access a protected dashboard. The system demonstrates basic backend concepts such as form handling, database interaction, and session management.
# PHP Authentication System

## Description
This project is a simple authentication system built with PHP and MySQL. 
It allows users to register, login, and logout securely.

## Features
- User registration
- User login
- Password hashing
- Session management
- Logout system

## Technologies
- PHP
- MySQL
- HTML/CSS

## Installation
1. Clone the repository:
git clone https://github.com/sabriiine15/authentification-system-php.git

2. Move the project to htdocs (XAMPP)

3. Import the database (database.sql) into phpMyAdmin

4. Start Apache and MySQL

5. Open in browser:
http://localhost/authentification-system-php

## Database

1. Create a new database in phpMyAdmin.
2. Import the database.sql file included in the project.
3. Make sure the database name matches the one used in the configuration file.
